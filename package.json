{
  "name": "node-react-app",
  "version": "1.0.0",
  "scripts": {
    "clean": "kill-port --port 9000,3000",
    "client": "cd client && npm install && npm start",
    "server": "cd api && npm install && npm start",
    "dev": "concurrently \"npm run clean\" \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^4.0.1",
    "kill-port": "^1.6.1"
  }
}
